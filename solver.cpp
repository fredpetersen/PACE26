class Solver {
  public:
    int solve() {
        // Implementation of the solver goes here
        // Instance instance;
        // // Populate instance with parsed data
        // instance = parseInput();

        // if (instance.trees == 2) {
        //   TwoTreeSolver solver = new TwoTreeSolver(instance.trees[0], instance.trees[1]);
        //   solver.solve()
        // } else {
        //   GeneralCaseSolver solve = new GeneralCaseSolver(instance)
        // }
        return 0;
    }
};